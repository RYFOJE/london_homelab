# ClusterIssuers + the lab wildcard Certificate. Wave 1: cert-manager (-5)
# owns the CRDs, and the Certificate lands in the traefik namespace, which
# Traefik (0) creates. The Cloudflare token Secret the issuers reference is
# pulled from Key Vault by the secrets app (-6).
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: tls
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  project: default
  source:
    repoURL: https://github.com/RYFOJE/london_homelab.git
    targetRevision: HEAD
    path: cluster/lab/tls
  destination:
    server: https://kubernetes.default.svc
    namespace: traefik
  syncPolicy:
    automated: { prune: true, selfHeal: true }
    retry:
      limit: 5
      backoff: { duration: 30s, factor: 2, maxDuration: 5m }
