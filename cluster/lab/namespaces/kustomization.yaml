apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Cluster-scoped, so no namespace: here. Adding a component: add its
# namespace below BEFORE the Application that lands in it, or the app's
# CreateNamespace=true creates it unlabelled and this app adopts it later.
resources:
  - namespaces.yaml
